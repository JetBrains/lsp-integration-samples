name: Build TypeScript LSP plugin

on:
  push:
    paths:
      - 'plugins/typescript-lsp-support/**'
      - '.github/workflows/build-typescript-plugin.yml'
  pull_request:
    paths:
      - 'plugins/typescript-lsp-support/**'
      - '.github/workflows/build-typescript-plugin.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out sources
        uses: actions/checkout@v5

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Build plugin variants
        working-directory: plugins/typescript-lsp-support
        run: ./gradlew buildPluginVariants --no-daemon

      - name: Upload Linux ARM64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-linux-arm64
          path: plugins/typescript-lsp-support/build/distributions/*-linux-arm64.zip
          if-no-files-found: error

      - name: Upload Linux x86_64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-linux-x86_64
          path: plugins/typescript-lsp-support/build/distributions/*-linux-x86_64.zip
          if-no-files-found: error

      - name: Upload macOS ARM64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-mac-arm64
          path: plugins/typescript-lsp-support/build/distributions/*-mac-arm64.zip
          if-no-files-found: error

      - name: Upload macOS x86_64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-mac-x86_64
          path: plugins/typescript-lsp-support/build/distributions/*-mac-x86_64.zip
          if-no-files-found: error

      - name: Upload Windows ARM64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-windows-arm64
          path: plugins/typescript-lsp-support/build/distributions/*-windows-arm64.zip
          if-no-files-found: error

      - name: Upload Windows x86_64 plugin
        uses: actions/upload-artifact@v5
        with:
          name: typescript-lsp-windows-x86_64
          path: plugins/typescript-lsp-support/build/distributions/*-windows-x86_64.zip
          if-no-files-found: error
